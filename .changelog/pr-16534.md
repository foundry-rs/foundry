---
foundry-common: patch
---

Fixed `console.log("%<n>e", ...)` panicking (aborting the whole `forge test` run) on a large explicit exponential precision, and silently producing wrong digits for a narrower precision range; large precisions are now rejected explicitly instead of crashing or misrepresenting the value.
