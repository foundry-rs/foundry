---
forge: minor
---

Added a confirmation prompt before executing project-configured local Solc and Vyper binaries, with `--allow-local-compiler` for explicit automation opt-in. Project dotenv files can no longer configure the browser executable.
