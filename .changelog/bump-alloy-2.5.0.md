---
anvil: patch
cast: patch
forge: patch
chisel: patch
---

Updated Alloy to 2.5.0. Anvil's `trace_filter` now requires both `fromAddress` and `toAddress` to match by default instead of matching either list.
