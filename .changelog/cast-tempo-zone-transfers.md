---
cast: minor
---

Added `cast tempo zone deposit` and `withdraw` with encrypted deposits, wallet-signed zone RPC authorization, optional token approval, L1 settlement waiting, and encrypted Earn deposit/redeem callback builders.
