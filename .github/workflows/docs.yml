name: docs

permissions: {}

on:
  workflow_call:
    inputs:
      artifact-id:
        description: Approved source artifact from the caller's dependency gate
        required: true
        type: string
      sha256:
        description: Approved source bundle checksum
        required: true
        type: string

concurrency:
  group: docs-${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  RUST_BACKTRACE: full
  RUSTC_WRAPPER: "sccache"

jobs:
  docs:
    runs-on: depot-ubuntu-latest
    timeout-minutes: 30
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Secure runner
        uses: tempoxyz/gh-actions/actions/secure-runner@09dddc697b1c8c1d3c3733b41d6fdd2274f17734
        with:
          egress-policy: audit
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-build
        id: setup-build
        with:
          artifact-id: ${{ inputs.artifact-id }}
          sha256: ${{ inputs.sha256 }}
          toolchain: nightly
      # Restore only build outputs, after verifying this run's approved sources.
      - name: Restore docs build cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        env:
          DOCS_CACHE_KEY: docs-v1-${{ steps.setup-build.outputs.cache-key }}-${{ hashFiles('Cargo.lock', 'Cargo.toml', 'crates/**/Cargo.toml', 'benches/**/Cargo.toml', '.cargo/**', '.github/actions/setup-build/**', '.github/actions/restore-dependencies/**', '.github/scripts/dependencies.mjs', '.github/scripts/solc-releases.mjs', '.github/workflows/docs.yml') }}
        with:
          path: target
          key: ${{ env.DOCS_CACHE_KEY }}-${{ github.sha }}
          restore-keys: ${{ env.DOCS_CACHE_KEY }}-
      - name: Build documentation
        run: rustup run nightly cargo doc --workspace --all-features --no-deps --document-private-items --frozen
        env:
          RUSTDOCFLAGS: --cfg docsrs -D warnings --show-type-layout --generate-link-to-definition --enable-index-page -Zunstable-options
      - name: Setup Pages
        if: github.ref_name == 'master' && github.event_name == 'push'
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - name: Upload artifact
        if: github.ref_name == 'master' && github.event_name == 'push'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: ./target/doc

  deploy-docs:
    if: github.ref_name == 'master' && github.event_name == 'push'
    needs: [docs]
    runs-on: depot-ubuntu-latest
    timeout-minutes: 30
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Secure runner
        uses: tempoxyz/gh-actions/actions/secure-runner@09dddc697b1c8c1d3c3733b41d6fdd2274f17734
        with:
          egress-policy: audit
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
