on:
  push:
    branches:
      - master
  pull_request:

name: test

jobs:
  cross-platform:
    name: Cross-platform tests
    needs: [ integration, lint, doc, unit ]
    uses: ./.github/workflows/cross-platform.yml

